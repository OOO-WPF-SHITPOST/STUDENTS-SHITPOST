<Window x:Class="Nedelyaeva.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Учет оценок" Height="200" Width="500">
    <Grid Margin="10">
        <StackPanel x:Name="LoginPanel" VerticalAlignment="Center" HorizontalAlignment="Center" Width="350">
            <Label Content="Логин:" />
            <TextBox x:Name="LoginTextBox" />
            <Label Content="Пароль:" Margin="0,10,0,0" />
            <PasswordBox x:Name="PasswordBox" />
            <Button Content="Войти" Width="120" Margin="0,10,0,0" Click="Login_Click" />
        </StackPanel>
    </Grid>
</Window>